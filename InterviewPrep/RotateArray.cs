using System;

namespace InterviewPrep
{
    public class RotateArray : IQuestion
    {
        public string Command => "rotate-array";
        public string Description => @"Given an array, rotate it to the right by k steps where k is non-negative.";

        public void Run()
        {
            throw new NotImplementedException();
        }
    }
}
